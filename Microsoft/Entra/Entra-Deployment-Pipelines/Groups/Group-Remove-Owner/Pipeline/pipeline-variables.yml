variables:
########################################################################################################
  # Environment - general. 
  # REQUIRED: Update the serviceConnection value to match your Azure DevOps service connection name.
########################################################################################################
  serviceConnection: '<YOUR-SERVICE-CONNECTION-NAME>'
  scriptPath: "$(Build.SourcesDirectory)/Entra/Groups/Group-Remove-Owner/Scripts/Remove-Owner-from-Group.ps1"

########################################################################################################
  # Group & user Properties. 
  # REQUIRED: Edit the groupName variable to use the display name for the group you want to remove users from.
  # REQUIRED: Edit the users variable to use the userPrincipalName for the users you want to remove from the group.
  # The users variable must maintain the provided JSON format.
  # Users Example:
  #   {
  #     "users": [
  #       {
  #         "userPrincipalName": "user@yourcompany.com"
  #       },
  #       {
  #         "userPrincipalName": "user@yourcompany.com"
  #       }
  #     ]
  #   }
########################################################################################################

  # Group to remove user(s)
  groupName: "<YOUR-GROUP-NAME>"

  # User(s) to remove from group
  users: >
    {
      "users": [  
        {
            "userPrincipalName": "user1@yourcompany.com"
        },
        {
            "userPrincipalName": "user2@yourcompany.com"
        }
      ]
    }