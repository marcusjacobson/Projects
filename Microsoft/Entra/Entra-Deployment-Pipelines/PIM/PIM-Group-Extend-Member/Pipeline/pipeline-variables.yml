variables:
########################################################################################################
  # Environment - general. 
  # REQUIRED: Update the serviceConnection value to match your Azure DevOps service connection name.
########################################################################################################
  serviceConnection: '<YOUR-SERVICE-CONNECTION-NAME>'
  scriptPath: "$(Build.SourcesDirectory)/Entra/PIM/PIM-Group-Extend-Member/Scripts/Extend-Member-for-PIM-Group.ps1"

########################################################################################################
  # PIM Group Properties. 
  # REQUIRED: Edit the groupName variable to use the display name for the group you want to extend PIM user assignments.
  # REQUIRED: Edit the users variable to use the userPrincipalName for the users you want to extend access to the PIM group.
  # The users variable must maintain the provided JSON format.
  # Users Example:
  #   {
  #     "users": [
  #       {
  #         "userPrincipalName": "user@yourcompany.com"
  #       },
  #       {
  #         "userPrincipalName": "user@yourcompany.com"
  #       }
  #     ]
  #   }
########################################################################################################

  # Group to extend user(s)
  groupName: "<YOUR-GROUP-NAME>"

  # User(s) to extend for the group
  users: >
    {
      "users": [  
        {
            "userPrincipalName": "user1@yourcompany.com"
        },
        {
            "userPrincipalName": "user2@yourcompany.com"
        }
      ]
    }